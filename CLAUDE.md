# typeburst

ブラウザで動く、演出が派手で爽快感のあるタイピングゲーム。見た目はシンプル（monkeytype風の暗い背景＋等幅フォント）、入力時の演出と音は派手に。静かな見た目と派手な演出の落差を狙う。

## 技術方針
- HTML/CSS/JavaScriptのみ。ビルド不要で、GitHub Pagesでそのまま公開できること
- 効果音・BGMは外部ファイルを使わず、Web Audio APIで合成する
- 外部スクリプトは使わない（フォントのみGoogle Fonts）
- PCのキーボードとスマホのソフトウェアキーボードの両方で遊べること
- 保存はlocalStorage（try/catchで囲む）

## 現在の仕様
- 出題：英単語、30秒／60秒のタイムアタック。スペースで次の単語へ
- 難易度：ノーマル／ハード。ハードは1回ミス（打ち間違い・不正解の単語確定）でコンボ・色段階・フィーバー・ゲージ・コード進行をすべて最初に戻す（`hardReset`）。代わりにスコア×1.5。HUDに hard と表示
- 設定：難易度、時間、音あり／なし、演出の強さ（弱・中・強）、テーマ。prefers-reduced-motion時は演出を弱に
- テーマ：ナイト・ペーパー（明るい背景）・フォレスト・エンバー・オーシャンの5種。背景・文字色とコンボ5段階の色をセットで切り替える（`THEMES`）
- コンボ段階：20・50・100・200で色が5段階に変化し（ナイトでは 水色→紫→ピンク→金→ミント）、倍率 ×1〜×5
- スコア：1打 10点×倍率、単語完成 (文字数×25＋連続正解×5)×倍率（倍率はハードの×1.5込み、端数は四捨五入）。単語ごとにダメージ数値表示（同時3つまで）
- 打鍵の音楽化：Am→F→C→G のコード進行に沿って1打ごとに音が鳴る。通常時は16打でコードが進む。コンボ20でベース、50で和音が加わる
- フィーバーゲージ：正解で増え、ミスで減る。75%以上でせり上がり音、満タンでドロップしてフィーバー突入
- フィーバー3段階：fever → super fever → hyper fever（倍率 ×2〜×4、BPM 128/140/152）。フィーバー中はゲージが徐々に減り、0で1段階下がる
- 文字の粉砕：完成した単語が文字ごとに砕け、スコアに吸い込まれて加点される
- カメラ演出：段階アップ時のズーム、hyper中はビートで脈打つ、RGBずれ
- 結果画面：スコアのカウントアップ、WPM、正確率、最大コンボ、最高ステージ、フィーバー時間、自己ベスト

## 作業ルール
- 1つの変更ごとにコミットし、何を変えたか日本語で簡潔に説明する
- 演出の追加は「演出の強さ」設定の中に収め、弱では控えめにする
- 仕様を変えたら、このファイルの「現在の仕様」も更新する
