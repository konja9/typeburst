# typeburst

ブラウザで動く、演出が派手で爽快感のあるタイピングゲーム。見た目はシンプル（monkeytype風の暗い背景＋等幅フォント）、入力時の演出と音は派手に。静かな見た目と派手な演出の落差を狙う。

## 技術方針
- HTML/CSS/JavaScriptのみ。ビルド不要で、GitHub Pagesでそのまま公開できること
- 効果音・BGMは外部ファイルを使わず、Web Audio APIで合成する
- 外部スクリプトは使わない（フォントのみGoogle Fonts）
- PCのブラウザで遊ばれる想定。操作性と見た目はPC（幅1024px以上で、ヘッダーが1行に収まること）を優先して決める
- スマホのソフトウェアキーボードでも遊べる状態は保つ（新機能はスマホで壊れていないことだけ確かめる）
- 保存はlocalStorage（try/catchで囲む）

## 現在の仕様
- 出題モード：英単語（`WORD_LIST` からランダム）／英文（`SENTENCES`：自作の短い英文200本、小文字・句読点なし。シャッフルして順に流す）／ローマ字（`JA_SENTENCES`：自作の短い日本語文200本、「表示|読み」でスペースが文節の区切り）。30秒／60秒のタイムアタック。スペースで次の単語へ
- 難易度：ノーマル／ハード／ハードコア。ノーマルはミスでコンボの段階が1つ下がり（`comboDown`、コンボ数はその段階の入口へ）、ゲージが減る（打ち間違い−12、不正解の確定−8）。ハードは1回ミス（打ち間違い・不正解の単語確定）でコンボ・色段階・フィーバー・ゲージ・コード進行をすべて最初に戻す（`hardReset`）。代わりにスコア×1.5。ハードコアは1回ミスでその時点で終了（`failRun`→約0.65秒後に結果へ）、スコア×2。WPMとWPM推移は終了までの経過時間で計算し、結果に「ミスで終了（○秒）」と表示。HUDに hard／hardcore と表示。説明文は `MODE_RULE` に1か所でまとめ、開始前の案内（`#ruleHint`）とドクロボタンの説明に使う。ハード／ハードコアを初めて選んだときだけ説明カード（`#ruleCard`、既読は `settings.seenRules`）を出し、OK・キー入力で閉じる
- ローマ字モード：1文節＝1単語。文節を打ち切るとスペースなしで自動確定。間違ったキーは入力を進めない。複数の綴りを受け付け（shi/si、chi/ti、tsu/tu、fu/hu、ji/zi、拗音の分け打ち kilya、っ は子音重ね・ltu/xtu、ん は nn/xn と子音の前の n、ー は -）、ガイドは打った綴りに合わせて変わる（`kanaUnits`・`romaMatch`）。は・を・へ は ha・wo・he。単語の上に今の文（漢字かな交じり・かな）を表示し、今の文節を強調
- 設定：出題モード、難易度、時間、音あり／なし、演出の強さ（弱・中・強）、テーマ。prefers-reduced-motion時は演出を弱に
- ヘッダー：時間（30s／60s）と難易度（ドクロ ×1.5＝ハード、ドクロ2つ ×2＝ハードコア、選択中を押すとノーマルへ）を1つの枠にまとめ、ハードで枠が赤、ハードコアで太く脈打つ。音は音符アイコン1つで切り替え（オフで斜線）、横に音量スライダー（`settings.vol`、0で音なし、上げると音ありに戻る）。演出の強さは「演出」の文字と高さの違う3本の棒のメーター（押した棒まで点灯、マウスを乗せると押したときの範囲を表示）で、押すとその強さのお試し演出が出る
- テーマ：ナイト・モノクロ・ペーパー・クリーム・オーシャン・フォレスト・トワイライト・サクラ・エンバー・サイバーの10種（ペーパーとクリームは明るい背景）。背景・文字色とコンボ5段階の色をセットで切り替える（`THEMES`、並びは `THEME_ORDER`）。ヘッダーの「テーマ」ボタンで一覧パネルを開いて選ぶ（外側クリック・Escで閉じる）。各テーマにモチーフのアイコン（`THEME_ICON`：三日月・半円・紙・カップ・波・葉・夕日・桜・炎・稲妻）があり、パネルの見本とヘッダーのボタンに使う
- コンボ段階：10・25・50・100で色が5段階に変化し（ナイトでは 水色→紫→ピンク→金→ミント）、倍率 ×1〜×5
- スコア：1打 10点×倍率、単語完成 (文字数×25＋連続正解×5)×倍率（倍率はハード×1.5・ハードコア×2込み、端数は四捨五入）。単語ごとにダメージ数値表示（同時3つまで）
- 打鍵の音楽化：Am→F→C→G のコード進行に沿って1打ごとに音が鳴る。通常時は16打でコードが進む。コンボ段階2（10）でベース、段階3（25）で和音が加わる。フィーバー中は打鍵のメロディを曲の32分音符の区切りに合わせて鳴らす（`quantT`、カチッという打鍵音は即時）。フィーバーの曲はキック以外を専用の経路（`musicBus`→`musicLP`）に通し、キックのたびに音量を一瞬下げて戻す（サイドチェイン、上の段階ほど深い）。ミスすると曲がこもって約1.1秒で戻る（`muffle`）。コンボ段階アップ・ドロップ・CRITICAL・NEW RECORD・結果のファンファーレには残響（ノイズから作った響きの ConvolverNode、`verbBus`）をかける。単語完成の音は連続正解で上がり、和音の音を使い切ると下に戻って高い倍音を1段ずつ重ねる（上がり続けて聞こえる）。ultra 以上は音が重なるので、曲の経路の高音を段階ごとに1.5dB削り（`musicShelf`）、戻す音量も少し下げる
- フィーバーゲージ：正解で増え、ミスで減る。75%以上でせり上がり音、満タンでドロップしてフィーバー突入
- フィーバー5段階：fever → super fever → hyper fever → ultra fever → LIMIT BREAK（倍率 ×2〜×6、BPM 128/140/152/164/176）。上の段階ほどゲージが貯まりにくく（`GAIN_KEY`）、早く減る（`DRAIN`）。数値は到達シミュレーションで調整（60WPM・正確率98%で、60秒なら7割ほどが LIMIT BREAK に届き、最初のフィーバーは約10秒）。フィーバー中はゲージが徐々に減り、0で1段階下がる。ultra でビネットの脈動・高音アルペジオ・オープンハイハット、LIMIT BREAK で小節頭の軽いフラッシュ（3回/秒未満）・強いRGBずれ・8分の和音の突き刺しが加わる
- CRITICAL：4%の単語が金色で出る（最初の3単語は除く、`crit[i]`）。打ち切ると得点×3、専用の演出と音。途中でミスすると金色が消える。結果画面に回数を表示
- 自己ベスト追い越し：開始時に区分の自己ベストを取り、HUDに「BEST まで ○」を表示。超えた瞬間に一度だけ NEW RECORD の演出と短いファンファーレ
- 文字の粉砕：完成した単語が文字ごとに砕け、スコアに吸い込まれて加点される
- カメラ演出：段階アップ時のズーム、hyper中はビートで脈打つ、RGBずれ
- ヒットストップ：段階アップと CRITICAL で一瞬（中80ms・強110ms）粒子などの動きを止めてズームし、解除と同時に弾ける。弱・reduced-motionでは無し
- カットイン：LIMIT BREAK 到達時に斜めの帯と巨大な文字が画面を横切る（0.9秒）。弱・reduced-motionでは文字のフェードだけ
- 結果画面：スコアのカウントアップ、WPM、正確率、最大コンボ、最高ステージ、フィーバー時間、自己ベスト（難易度×時間ごと）
- 結果の詳細：WPMの推移（1秒ごと・3秒移動平均）をインラインSVGの折れ線で表示、ミスの多いキー上位5件。結果・記録画面は縦スクロールできる
- シェア：結果画面と記録画面の自己ベストのカードに「コピー」「Xに投稿」（x.com/intent/post）と、対応端末では「共有」（navigator.share）。文は記録1件から `runShareText(run, 見出し)` で作り、結果は「今回の結果」、カードは「自己ベスト」を先頭に付ける（古い記録でない項目は省く）。スコア・出題モード・難易度・時間・WPM・正確率・最大コンボ・最高ステージ・ページURLをまとめたテキスト
- 遊び方：ヘッダーの「?」で開く画面（`#help`）。難易度・コンボ・フィーバー・スコア・出題モード・操作を、ゲームの定数（`MODE_RULE`・`TIER_MULT`・`STAGE_MULT` など）から作って表示。記録画面と同じ開閉の仕組み（`openView`／`closeView`）で、Escで閉じ、プレイ中に開くとそのゲームは中断
- スコア記録：`typeburst-records` に保存。出題モード×難易度×時間の区分ごとの上位10件（ランキング）と直近20件（履歴）を残す。記録画面は「出題・難易度・時間」の3つの絞り込み、選んだ区分の自己ベストのカード（スコア・WPM・正確率・最大コンボ・最高ステージ・日付）、「ランキング」（2〜10位、1位との比率の棒グラフ）と「最近のプレイ」（全区分の直近20件、区分のチップ付き）のタブ。今回のプレイは強調表示。出題モードがない古い記録は英単語として扱う。ヘッダーの「記録」／結果画面の「記録を見る」で開き、Escで閉じる。以前の `typeburst-bestscore-30/60` はノーマルの記録として引き継ぐ

- タブのアイコン：`favicon.svg`（暗い角丸の正方形に水色のカーソルと小さな花火）、`favicon-32.png`、`icon-180.png`（ホーム画面用、角なし）。PNGはSVGから描画して作ったもの。表示中はテーマの色で作り直したSVGに差し替える（`faviconSVG`）

- リンクのプレビュー：`index.html` の `<head>` に OGP と Twitter カードのメタタグ。画像は `og-image.png`（1200×630、プレイ画面をイメージした静止画）。URLは公開先の絶対URL

- 描画の負担：演出は全画面キャンバス `#fx` に描く。スピードラインは1本のパスにまとめて1回で描く（最大240本）

## 作業ルール
- 1つの変更ごとにコミットし、何を変えたか日本語で簡潔に説明する
- 演出の追加は「演出の強さ」設定の中に収め、弱では控えめにする
- 仕様を変えたら、このファイルの「現在の仕様」も更新する
